export function ModulePlaceholder({ title }: { title: string }) {
  return (
    <div className="placeholder-pane">
      <h2>{title}</h2>
      <p>Em construção — chega numa próxima fase.</p>
    </div>
  );
}
